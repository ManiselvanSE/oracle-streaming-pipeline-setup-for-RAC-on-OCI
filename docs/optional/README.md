# Optional Configurations

These configs are **not required** for the core demo, which uses Kafka Connect in **standalone** mode.

| File | Purpose |
|------|---------|
| `connect-distributed-kraft.properties` | Use if you prefer **distributed** Connect instead of standalone. Run from project root: `connect-distributed -daemon docs/optional/connect-distributed-kraft.properties` |
